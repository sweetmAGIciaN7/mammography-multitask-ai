# Data

Place local mammography datasets in this directory.

Do not commit images, labels, or other dataset files. Dataset locations should be configured locally and are not stored in the repository.
